import React from "react";
import "./header.css";
import { Typography } from "@mui/material";

function Header() {
  return (
    <div class="header">
      <Typography fontSize={35}>QuizApp</Typography>
    </div>
  );
}

export default Header;
